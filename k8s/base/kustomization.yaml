apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The namespace is applied explicitly in Tiltfile before Helm charts are
# rendered. Production overlays can include k8s/base/namespace directly.
resources: []
