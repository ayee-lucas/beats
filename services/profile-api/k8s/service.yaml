apiVersion: v1
kind: Service
metadata:
  name: profile-api
  labels:
    app: profile-api
spec:
  type: ClusterIP
  selector:
    app: profile-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
